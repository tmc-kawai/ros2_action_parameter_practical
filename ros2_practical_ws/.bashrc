# Auto-loaded inside the practical container (HOME=/ros2_ws).
# No host home is mounted, so nothing here depends on the host environment.
source /opt/ros/jazzy/setup.bash
[ -f /ros2_ws/install/setup.bash ] && source /ros2_ws/install/setup.bash
export PS1='[jazzy ws] \w$ '
